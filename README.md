# bittuly
Distibuted URL Shortener

# Docker commands
```bash
docker compose up -d
```

This starts PostgreSQL on `localhost:5432` with:

- username: `bittu`
- password: `bittu`
- database: `bittuly`
- tables: `users`, `urls`
